run = "python3 -m http.server ${PORT:-3000} --bind 0.0.0.0"
modules = ["nodejs-20", "web", "nix"]

[nix]
channel = "stable-25_05"

[[ports]]
localPort = 3000
externalPort = 80

[agent]
expertMode = true
